Periapsis
=========

.. _periapsis:

Periapsis (:math:`a (1 - e)`) is the nearest distance between a child and a parent body.
It is determined by the :ref:`semi-major axis <semi_major_axis>` :math:`a` and the
:ref:`eccentricity <eccentricity>` :math:`e`.
